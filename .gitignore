# Banco de pruebas local del visor: embebe los 132 laboratorios reales
# completos en un atributo HTML a mano (pesa ~300KB) — es solo para probar
# en local con Live Server, no debe subirse al repo.
visor/prueba.html
